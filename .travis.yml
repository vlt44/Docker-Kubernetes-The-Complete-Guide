sudo: required
services:
  - docker

before_install:
  - docker build -t vltaylor/docker-kubernetes-the-complete-guide -f flow/frontend/Dockerfile.dev flow/frontend

script:
  - docker run -e CI=true vltaylor/docker-kubernetes-the-complete-guide npm run test -- --coverage
