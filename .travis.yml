sudo: required
services:
  - docker

before_install:
  - docker build -t vltaylor/Docker-Kubernetes-The-Complete-Guide -f Dockerfile.dev .

script:
  - docker run -e CI=true vltaylor/Docker-Kubernetes-The-Complete-Guide npm run test -- --coverage
